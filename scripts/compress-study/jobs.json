{
    "loader" : "latf-load",
    "transform" :
    [
        { "${ENCODE-RAW_NAME}" : "< ascii > zstd_encoding<5, 11>",
          "${ENCODE-READ}" : "<INSDC:2na:packed> zip_encoding",
          "${ENCODE-ALTREAD}" : "< INSDC:4na:bin > zip_encoding"
        }
    ]
}
