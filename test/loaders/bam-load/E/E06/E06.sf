#bam-load error test #06 : an aligment-name appears as unaligned once and 2 times as aligned

r:type=random,name=R1,length=5000

# write all references into one file as FASTA
ref-out:E06.fasta

# write the final SAM-output into this file
sam-out:E06.sam

# write a config-file for bam-load
config:E06.config

u:name=A1,len=50
p:name=A1,pos=100
p:name=A1,pos=300

#some additional ( correct ) alignments
p:name=B,repeat=50
p:name=B,repeat=50

