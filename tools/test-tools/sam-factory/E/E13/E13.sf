#bam-load error test #13 : a pair of alignments has a secondary alignment - but the length differ

r:type=random,name=R1,length=5000

# write all references into one file as FASTA
ref-out:E13.fasta

# write the final SAM-output into this file
sam-out:E13.sam

# write a config-file for bam-load
config:E13.config

p:name=A1,pos=100,cigar=30M
p:name=A1,pos=300,cigar=20M
s:name=A1,pos=200,cigar=25M

#some additional ( correct ) alignments
p:name=B,repeat=50
p:name=B,repeat=50
